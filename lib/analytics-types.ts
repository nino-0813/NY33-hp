/** GA4 計測で使うページ種別（ルートパスから判定） */
export type PageType = "blog" | "service" | "faq" | "top"

/** ブログ記事内 CTA の配置 */
export type BlogCtaPosition = "top" | "bottom" | "inline"

/** ブログ CTA の種類 */
export type BlogCtaType = "line" | "contact"
